[atcoder]
submit_via_binary = true
update_interval = 1000

[profile]
target = "x86_64-unknown-linux-musl"

[profile.release]
lto = true
panic = 'abort'

[dependencies]

[project]
# rustc_version = "1.15.1"

template = """
fn main() {
    unimplemented!();
}
"""
